# 客户管理订单状态分离实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将客户管理分为"订单创建"和"订单跟进"两个状态，实现签单金额门槛和订单流转可视化。

**Architecture:** 在 orders 表新增 signed_amount 和 final_order_amount 字段；在 customers 表新增 order_stage 和 has_active_order 字段；改造客户管理页面为 Tab 切换；改造转交设计流程增加签单金额必填；在下单工厂前增加签单金额校验。

**Tech Stack:** Next.js, Supabase, TypeScript, TailwindCSS, shadcn/ui

---

## 文件结构

```
src/
├── app/(dashboard)/customers/page.tsx          # 改造：增加Tab切换
├── app/(dashboard)/customers/[id]/page.tsx     # 改造：显示订单阶段和金额
├── app/api/orders/route.ts                     # 改造：POST增加signed_amount
├── app/api/orders/[id]/dispatch/route.ts       # 改造：更新客户状态
├── app/api/orders/[id]/place-order/route.ts    # 改造：检查signed_amount
├── app/api/orders/[id]/submit-design/route.ts   # 改造：增加final_order_amount
├── components/customers/
│   ├── customer-list.tsx                       # 改造：过滤有订单的客户
│   ├── transfer-design-button.tsx               # 改造：增加签单金额输入
│   └── order-followup-list.tsx                 # 新建：订单跟进列表组件
└── lib/types.ts                                # 改造：Order接口增加字段

supabase/migrations/
└── 2026-04-23-add-order-amounts.sql           # 新建：数据库迁移
```

---

## 实现任务

### Task 1: 数据库迁移 - 新增订单金额和客户状态字段

**Files:**
- Create: `supabase/migrations/2026-04-23-add-order-amounts.sql`

- [ ] **Step 1: 创建迁移文件**

```sql
-- orders 表新增字段
ALTER TABLE orders ADD COLUMN IF NOT EXISTS signed_amount DECIMAL(12,2);
ALTER TABLE orders ADD COLUMN IF NOT EXISTS final_order_amount DECIMAL(12,2);

-- customers 表新增字段
ALTER TABLE customers ADD COLUMN IF NOT EXISTS order_stage TEXT DEFAULT NULL;
ALTER TABLE customers ADD COLUMN IF NOT EXISTS has_active_order BOOLEAN DEFAULT FALSE;
```

- [ ] **Step 2: 在 Supabase 执行迁移**

在 Supabase Dashboard 的 SQL Editor 中执行上述 SQL。

- [ ] **Step 3: 提交迁移文件**

```bash
git add supabase/migrations/2026-04-23-add-order-amounts.sql
git commit -m "db: add signed_amount, final_order_amount to orders and order_stage, has_active_order to customers"
```

---

### Task 2: 更新 TypeScript 类型定义

**Files:**
- Modify: `src/lib/types.ts:70-95`

- [ ] **Step 1: 更新 Order 接口**

在 Order 接口中添加 signed_amount 和 final_order_amount 字段：

```typescript
export interface Order {
  // ... existing fields ...
  signed_amount: number | null
  final_order_amount: number | null
  // ... rest of fields ...
}
```

- [ ] **Step 2: 更新 Customer 接口**

在 Customer 接口中添加 order_stage 和 has_active_order 字段：

```typescript
export interface Customer {
  // ... existing fields ...
  order_stage: string | null
  has_active_order: boolean
  // ... rest of fields ...
}
```

- [ ] **Step 3: 提交**

```bash
git add src/lib/types.ts
git commit -m "types: add signed_amount, final_order_amount, order_stage, has_active_order fields"
```

---

### Task 3: 改造转交设计按钮 - 增加签单金额输入

**Files:**
- Modify: `src/components/customers/transfer-design-button.tsx`

- [ ] **Step 1: 增加签单金额状态**

在组件顶部添加：
```typescript
const [signedAmount, setSignedAmount] = useState<string>('')
```

- [ ] **Step 2: 在弹窗中增加签单金额输入框**

在设计师选择列表下方添加：
```tsx
<div className="space-y-2">
  <label className="text-sm font-medium">签单金额（万）</label>
  <input
    type="number"
    value={signedAmount}
    onChange={(e) => setSignedAmount(e.target.value)}
    placeholder="请输入签单金额"
    className="w-full px-3 py-2 border rounded-lg"
  />
</div>
```

- [ ] **Step 3: 验证必填**

在 handleTransfer 函数开头添加：
```typescript
if (!signedAmount || parseFloat(signedAmount) <= 0) {
  alert('请填写签单金额')
  return
}
```

- [ ] **Step 4: 传递签单金额到 API**

修改 fetch 调用，添加 signed_amount：
```typescript
body: JSON.stringify({
  customer_name: customerName,
  customer_id: customerId,
  signed_amount: parseFloat(signedAmount),
}),
```

- [ ] **Step 5: 提交**

```bash
git add src/components/customers/transfer-design-button.tsx
git commit -m "feat: add signed_amount input to transfer design dialog"
```

---

### Task 4: 改造创建订单 API - 接收签单金额

**Files:**
- Modify: `src/app/api/orders/route.ts:64-68`

- [ ] **Step 1: 接收 signed_amount 参数**

在 POST 函数中添加 signed_amount 接收：
```typescript
const {
  customer_name, customer_phone, customer_address,
  house_type, house_area, signed_amount
} = body
```

- [ ] **Step 2: 创建订单时保存 signed_amount**

在 insert 语句中添加：
```typescript
signed_amount: signed_amount || null,
```

- [ ] **Step 3: 提交**

```bash
git add src/app/api/orders/route.ts
git commit -m "feat: save signed_amount when creating order"
```

---

### Task 5: 改造派单 API - 更新客户状态

**Files:**
- Modify: `src/app/api/orders/[id]/dispatch/route.ts`

- [ ] **Step 1: 派单成功后更新客户状态**

在派单成功（创建 notification 后）添加：
```typescript
// 更新客户状态为有进行中订单
await adminSupabase
  .from('customers')
  .update({
    has_active_order: true,
    order_stage: 'pending_design'
  })
  .eq('id', order.customer_id)
```

- [ ] **Step 2: 提交**

```bash
git add src/app/api/orders/[id]/dispatch/route.ts
git commit -m "feat: update customer has_active_order and order_stage on dispatch"
```

---

### Task 6: 改造客户管理页面 - 增加 Tab 切换

**Files:**
- Modify: `src/app/(dashboard)/customers/page.tsx`

- [ ] **Step 1: 获取两种状态的客户**

修改 getCustomers 函数获取两种客户：
```typescript
async function getCustomers() {
  // 获取无订单客户（订单创建）
  const [customersWithoutOrders, customersWithOrders] = await Promise.all([
    // 订单创建：客户没有进行中订单
    adminSupabase
      .from('customers')
      .select('*')
      .eq('organization_id', user.organization_id)
      .eq('has_active_order', false)
      .order('created_at', { ascending: false }),
    // 订单跟进：客户有进行中订单
    adminSupabase
      .from('customers')
      .select('*, orders(id, order_no, signed_amount, status, order_stage)')
      .eq('organization_id', user.organization_id)
      .eq('has_active_order', true)
      .order('created_at', { ascending: false }),
  ])
  return {
    withoutOrders: customersWithoutOrders.data || [],
    withOrders: customersWithOrders.data || []
  }
}
```

- [ ] **Step 2: 增加 Tab 状态**

添加 useState：
```typescript
const [tab, setTab] = useState<'create' | 'followup'>('create')
```

- [ ] **Step 3: 增加 Tab UI**

在页面中添加：
```tsx
<Tabs value={tab} onValueChange={(v) => setTab(v as 'create' | 'followup')}>
  <TabsList>
    <TabsTrigger value="create">订单创建</TabsTrigger>
    <TabsTrigger value="followup">
      订单跟进
      {customers.withOrders.length > 0 && (
        <Badge className="ml-2 bg-orange-500">{customers.withOrders.length}</Badge>
      )}
    </TabsTrigger>
  </TabsList>
</Tabs>
```

- [ ] **Step 4: 根据 Tab 渲染不同列表**

```tsx
{tab === 'create' ? (
  <CustomerList customers={customers.withoutOrders} />
) : (
  <OrderFollowupList customers={customers.withOrders} />
)}
```

- [ ] **Step 5: 提交**

```bash
git add src/app/\(dashboard\)/customers/page.tsx
git commit -m "feat: add tab switch between order creation and followup in customer management"
```

---

### Task 7: 创建订单跟进列表组件

**Files:**
- Create: `src/components/customers/order-followup-list.tsx`

- [ ] **Step 1: 创建组件**

```typescript
'use client'

import Link from 'next/link'
import { Card } from '@/components/ui/card'
import { Badge } from '@/components/ui/badge'

const STAGE_LABELS: Record<string, string> = {
  pending_dispatch: '待派单',
  pending_design: '待接单',
  designing: '设计中',
  pending_order: '待下单',
  pending_payment: '待打款',
  pending_shipment: '待出货',
  in_install: '安装中',
  completed: '已完结'
}

const STAGE_COLORS: Record<string, string> = {
  pending_dispatch: 'bg-gray-100 text-gray-700',
  pending_design: 'bg-orange-100 text-orange-700',
  designing: 'bg-blue-100 text-blue-700',
  pending_order: 'bg-purple-100 text-purple-700',
  pending_payment: 'bg-yellow-100 text-yellow-700',
  pending_shipment: 'bg-cyan-100 text-cyan-700',
  in_install: 'bg-green-100 text-green-700',
  completed: 'bg-gray-100 text-gray-500'
}

export function OrderFollowupList({ customers }: { customers: any[] }) {
  if (customers.length === 0) {
    return (
      <div className="text-center py-12 text-gray-500">
        暂无订单跟进中的客户
      </div>
    )
  }

  return (
    <div className="space-y-4">
      {customers.map((customer) => {
        const order = customer.orders?.[0]
        return (
          <Link key={customer.id} href={`/customers/${customer.id}`}>
            <Card className="p-4 hover:bg-gray-50 transition-colors cursor-pointer">
              <div className="flex items-center justify-between mb-2">
                <h3 className="font-medium">{customer.name}</h3>
                {order && (
                  <Badge className={STAGE_COLORS[order.status] || 'bg-gray-100'}>
                    {STAGE_LABELS[order.status] || order.status}
                  </Badge>
                )}
              </div>
              <p className="text-sm text-muted-foreground mb-2">
                {customer.phone} · {customer.house_type || '未填写房型'}
              </p>
              {order && (
                <div className="flex items-center justify-between text-sm">
                  <span className="text-muted-foreground">
                    订单号: {order.order_no}
                  </span>
                  {order.signed_amount && (
                    <span className="text-green-600 font-medium">
                      签单: ¥{order.signed_amount}万
                    </span>
                  )}
                </div>
              )}
            </Card>
          </Link>
        )
      })}
    </div>
  )
}
```

- [ ] **Step 2: 提交**

```bash
git add src/components/customers/order-followup-list.tsx
git commit -m "feat: add OrderFollowupList component for customer followup view"
```

---

### Task 8: 改造提交方案 API - 增加最终下单销售额

**Files:**
- Modify: `src/app/api/orders/[id]/submit-design/route.ts`

- [ ] **Step 1: 接收 final_order_amount 参数**

在函数开头添加：
```typescript
const body = await request.json()
const { final_order_amount } = body
```

- [ ] **Step 2: 更新订单**

在更新订单处添加：
```typescript
if (final_order_amount) {
  updates.final_order_amount = parseFloat(final_order_amount)
}
```

- [ ] **Step 3: 提交**

```bash
git add src/app/api/orders/[id]/submit-design/route.ts
git commit -m "feat: add final_order_amount field to submit-design API"
```

---

### Task 9: 改造下单工厂 API - 检查签单金额门槛

**Files:**
- Modify: `src/app/api/orders/[id]/place-order/route.ts`

- [ ] **Step 1: 获取订单并检查签单金额**

在函数开头添加检查：
```typescript
const { data: order } = await adminSupabase
  .from('orders')
  .select('*')
  .eq('id', params.id)
  .single()

if (!order) {
  return NextResponse.json({ error: '订单不存在' }, { status: 404 })
}

// 检查签单金额
if (!order.signed_amount || order.signed_amount <= 0) {
  // 获取订单创建者（导购）的 ID
  const { data: customer } = await adminSupabase
    .from('customers')
    .select('created_by')
    .eq('id', order.customer_id)
    .single()

  // 发送通知给导购
  if (customer?.created_by) {
    await adminSupabase.from('notifications').insert({
      organization_id: order.organization_id,
      user_id: customer.created_by,
      type: 'signed_amount_required',
      priority: 'urgent',
      title: '签单金额待填写',
      summary: `订单 ${order.order_no} 即将下单，请填写签单金额`,
      related_order_id: order.id
    })
  }

  return NextResponse.json({
    error: '签单金额未填写，请通知导购填写后再下单'
  }, { status: 400 })
}
```

- [ ] **Step 2: 提交**

```bash
git add src/app/api/orders/[id]/place-order/route.ts
git commit -m "feat: check signed_amount before placing order to factory"
```

---

### Task 10: 改造客户详情页 - 显示订单阶段和金额

**Files:**
- Modify: `src/app/(dashboard)/customers/[id]/page.tsx`

- [ ] **Step 1: 获取订单信息**

在 getCustomer 函数中添加订单查询：
```typescript
const { data: orders } = await adminSupabase
  .from('orders')
  .select('*')
  .eq('customer_id', customer.id)
  .eq('has_active_order', true)
  .order('created_at', { ascending: false })
```

- [ ] **Step 2: 在页面中显示**

添加订单阶段和金额显示区域。

- [ ] **Step 3: 提交**

```bash
git add src/app/\(dashboard\)/customers/\[id\]/page.tsx
git commit -m "feat: display order stage and amounts on customer detail page"
```

---

### Task 11: 订单完结时更新客户状态

**Files:**
- Modify: `src/app/api/orders/[id]/complete/route.ts`

- [ ] **Step 1: 完结时将客户状态重置**

在订单完结逻辑后添加：
```typescript
// 将客户的 has_active_order 设为 false
await adminSupabase
  .from('customers')
  .update({
    has_active_order: false,
    order_stage: 'completed'
  })
  .eq('id', order.customer_id)
```

- [ ] **Step 2: 提交**

```bash
git add src/app/api/orders/[id]/complete/route.ts
git commit -m "feat: reset customer has_active_order when order completes"
```

---

## 测试验证

完成所有任务后，执行以下测试：

1. **创建客户** → 确认显示在"订单创建" Tab
2. **转交设计** → 填写签单金额 → 确认客户出现在"订单跟进" Tab
3. **接单** → 确认订单阶段更新为"设计中"
4. **下单工厂** → 如果签单金额为空则阻止并提示
5. **订单完结** → 确认客户重新回到"订单创建" Tab
