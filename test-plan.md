# 测试方案：R2 文件存储集成 + 设计编辑权限扩展

## 测试环境

| 项目 | 说明 |
|------|------|
| 测试工具 | chrome-devtools MCP |
| 目标站点 | `http://localhost:3000`（本地开发服务器） |
| 前提 | `npm run dev` 已启动、`.env.local` 配置完整 |
| 测试账号 | 至少：1 个设计师 + 1 个非设计师（店长/销售） |

---

## 一、权限范围测试

> 验证 CAD 上传/删除在 **pending_design / designing / in_design / pending_order** 四种状态下可见可操作。

### TC-01: pending_design 状态 — 编辑表单可见

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 设计师登录，进入 `/designs` | 看到方案列表 |
| 2 | 点击一个关联订单为「待接单」的方案 | 进入详情页 |
| 3 | 检查页面 | 存在 **「编辑设计方案」** 卡片和「点击上传 CAD 文件」按钮 |
| 4 | 检查页面底部 | 存在 **「删除方案」** 按钮 |

### TC-02: designing 状态 — 编辑表单可见

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入关联订单为「设计中」的方案详情 | |
| 2 | 存在「编辑设计方案」卡片 | ✅ |

### TC-03: pending_order 状态 — 编辑表单 + 下单卡片同时可见

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入关联订单为「待下单」的方案详情 | |
| 2 | 存在「编辑设计方案」卡片 | ✅ |
| 3 | 存在「下单到工厂」卡片（PlaceOrderCard） | ✅ |

### TC-04: pending_payment 状态 — 不可编辑

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入关联订单为「待打款」的方案详情 | |
| 2 | 仅显示「方案信息」只读卡片 | ✅ |
| 3 | **不存在**「编辑设计方案」卡片和「删除方案」按钮 | ✅ |

### TC-05: 非分配设计师 — 不可编辑

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 用店长或销售登录，进入「设计中」的方案详情 | |
| 2 | 仅显示「方案信息」只读卡片 | ✅ |
| 3 | **不存在**编辑表单 | ✅ |

---

## 二、CAD 文件上传测试

### TC-06: 正常上传小文件（< 4.5MB）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 设计师进入可编辑方案，点击「点击上传 CAD 文件」 | 文件选择器弹出 |
| 2 | 选择一个 .dwg / .pdf 文件 | |
| 3 | 观察按钮状态 | 变为「上传中...」 |
| 4 | 上传完成 | 显示：文件名 + 「下载」链接 + X 按钮 |
| 5 | 点击「保存方案」 | 提示「保存成功」 |
| 6 | 刷新页面 | CAD 文件仍在 |
| 7 | Network 面板查 `/api/upload/sign` | 返回 `{ signedUrl, publicUrl, path, filename }` |
| 8 | Network 面板查 R2 PUT 请求 | `*.r2.cloudflarestorage.com` 返回 200 |

### TC-07: 上传中等文件（> 4.5MB，验证绕过 Vercel 限制）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 选择 10MB ~ 50MB 文件上传 | 上传成功（直传 R2） |
| 2 | 保存、刷新 | 文件仍在 |

### TC-08: 超大文件拒绝

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 选择 > 500MB 文件 | 前端立即报错「文件过大，最大支持 500MB」 |
| 2 | Network 检查 | 不应发出 `/api/upload/sign` 请求 |

### TC-09: 不支持的文件类型拒绝

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 选择 .exe 文件 | 报错「不支持的文件类型」 |

### TC-10: 未登录上传（API）

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 隐身窗口访问 `/api/upload/sign`，POST `{ filename: "t.dwg" }` | 返回 401 |

---

## 三、CAD 文件下载测试

### TC-11: 编辑页下载 — 预签名 URL 重定向

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 在编辑页点击 CAD 文件「下载」链接 | |
| 2 | Network 面板检查 | `/api/files/download?path=xxx` 返回 **302** |
| 3 | 重定向到 R2 预签名 URL | 200，文件下载成功 |
| 4 | 文件内容正确 | ✅ |

### TC-12: 安装详情页下载

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入 `/installations/[id]`，点击「下载 CAD 图纸」 | |
| 2 | Network 检查 | 302 → 预签名 URL → 下载成功 |

### TC-13: 完结订单详情页下载

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 进入 `/completed-orders/[id]`，点击「下载 CAD 图纸」 | |
| 2 | Network 检查 | 302 → 预签名 URL → 下载成功 |

### TC-14: 下载权限校验

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | Console: `fetch('/api/files/download?path=other-org-xxx/t.dwg')` | 返回 403 |
| 2 | Console: `fetch('/api/files/download')` | 返回 400 |

---

## 四、CAD 文件删除测试（核心）

### TC-15: 前端删除 → R2 同步删除

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 上传一个 CAD 文件并保存 | 文件显示在界面 |
| 2 | 点击 X 按钮 | 按钮变为「删除中...」 |
| 3 | Network 检查 `/api/files/delete` | 返回 200 `{ success: true }` |
| 4 | 界面更新 | 恢复为「点击上传 CAD 文件」 |
| 5 | 点击「保存方案」 | 成功 |
| 6 | 刷新页面 | CAD 文件区域为空 |
| 7 | 尝试通过旧路径下载 | R2 返回 NoSuchKey（文件已物理删除） |

### TC-16: 未保存就删除

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 上传文件后不点保存，直接点 X 删除 | |
| 2 | Network 确认 `/api/files/delete` 被调用 | ✅ |
| 3 | 界面恢复为上传按钮 | ✅ |

### TC-17: 删除权限校验

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | Console 发跨组织删除请求 | 返回 403 |

---

## 五、连贯流程测试

### TC-18: 上传 → 删除 → 再上传

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 上传文件 A（test_v1.dwg），保存 | 成功 |
| 2 | 刷新，确认文件 A 存在 | ✅ |
| 3 | 删除文件 A，保存 | 成功 |
| 4 | 刷新，确认文件 A 已消失 | ✅ |
| 5 | 上传文件 B（test_v2.dwg），保存 | 成功 |
| 6 | 刷新，确认文件 B 存在 | ✅ |
| 7 | 下载文件 B，校验内容 | 正确 |

### TC-19: 跨状态编辑

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 在 pending_design 状态上传 CAD 文件，保存 | 成功 |
| 2 | 订单流转到 designing | |
| 3 | 进入方案详情，删除 CAD 文件 | 成功 |
| 4 | 重新上传新文件，保存 | 成功 |

---

## 六、回归验证

### TC-20: 非 CAD 字段编辑

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 编辑标题、房间数量、总面积、描述、酷家乐链接 | |
| 2 | 保存，刷新 | 所有字段正确保存 |

### TC-21: 方案提交流程

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 点击「保存并提交方案」 | 跳转正确，方案变为「已提交」 |

### TC-22: 控制台无报错

| 步骤 | 操作 | 预期 |
|------|------|------|
| 1 | 以上所有测试过程中，保持 DevTools Console 打开 | 无 JS 报错、无 404/500 |

---

## 七、测试数据准备

| 用途 | 文件 |
|------|------|
| 上传小文件 | < 4.5MB 的 .dwg 或 .pdf |
| 上传中等文件 | 10-50MB 的 .pdf |
| 超大文件（拒绝） | > 500MB 文件 |
| 非法文件类型 | .exe 文件 |
| 方案数据 | 关联订单分别为 pending_design / designing / pending_order / pending_payment 的方案各一个 |

---

## 执行顺序

```
一、权限范围 (TC-01~05) → 确认功能可见性
二、上传流程 (TC-06~10) → 确认上传链路
三、下载流程 (TC-11~14) → 确认下载链路
四、删除流程 (TC-15~17) → 确认 R2 同步删除
五、连贯流程 (TC-18~19) → 端到端验证
六、回归验证 (TC-20~22) → 确认无破坏
```
